# bmad-loop orchestration state — transient worktrees, run logs, and archives.
# Regenerated by the bmad-loop tooling; never commit (run-archive tarballs are huge).
.bmad-loop/

# Node / Bun
node_modules/
dist/
dist-ssr/
*.local

# Wrangler / D1 local state (never commit the local D1 sqlite file)
.wrangler/
*.sqlite
*.sqlite3

# Secrets — never committed; provided via Wrangler secrets / GH Actions secrets
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# Python (legacy bootstrap scripts)
__pycache__/
*.pyc
.venv/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store

# Logs
*.log
npm-debug.log*
