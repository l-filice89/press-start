---
scope: Whole-app v1 architecture for PS Game Catalog (PRESS START): a single-user installable PWA (React SPA + Bun TS backend) replacing a Notion game-tracking DB, on free-tier hosting with an externally managed DB. Governs the four open questions (DB, PS auth, games DB, scheduled job) plus data/sync invariants.
purpose: build-substrate
altitude: feature
updated: 2026-07-17T15:45
---

- (version) Web-verified 2026-07-05: Cloudflare Workers FREE — 10ms CPU/invocation but network I/O (fetch/D1) does NOT count toward CPU; 50 subrequests/invocation (hard cap on free); native Cron Triggers (up to 5) included free; no wall-clock limit for HTTP workers. D1 free: 5GB, 5M rows read/day, 100K writes/day, 10GB/db. D1 accessed via binding = NOT a subrequest.
- (version) Web-verified 2026-07-05: Turso free — 5GB, 500M row-reads/mo, 100 DBs; meters rows-scanned. Convex free — native cron/scheduled fns, reactive doc model (not SQLite), $25/mo beyond free. psn-api (npm, JS/TS, pure fetch) — NPSSO(64-char, ~2mo life)→access+refresh token exchange; refresh token renews without re-fetching NPSSO. Sturdier than raw pdccws_p cookie; runtime-agnostic.
- (constraint) INHERITED (hard, from PRD/brief/project-context): free-tier hosting is a hard constraint and OUTRANKS the SQLite preference, which outranks the Bun-runtime preference. App must be stateless, data externally managed. Scheduled monthly PS+ Extra job must fit the free tier. Nothing external on render (NFR-3). Failures surface, never silently retry (NFR-4).
- (decision) [ADOPTED] Platform = Cloudflare, single vendor. One Worker serves the React SPA (Workers Static Assets) + the JSON API; DB = Cloudflare D1 (SQLite-flavored) accessed via binding (not a subrequest); scheduled work = Cloudflare Cron Triggers. Binds: NFR-1, NFR-2, all backend. Prevents: divergent hosting/DB/cron choices; a stateful server; an external cron pinger. Chosen over Convex (rewrites the SQLite data contract, highest lock-in, $25/mo floor) and Bun-on-free-host (sleeps, can't self-cron). Free-hosting hard constraint outranks the Bun preference.
- (decision) [ADOPTED] Bun demoted from runtime to LOCAL DEV TOOLCHAIN ONLY (package manager, test runner, out-of-band scripts). Deployed runtime is workerd/V8. TypeScript throughout. Overturns the project-context 'Bun runtime' preference — justified by the free-hosting>SQLite>Bun ranking. project-context.md must be updated to reflect this at finalize.
- (decision) Future-proofing lives in architecture, not vendor: persistence sits behind a repository/data-access boundary; schema is relational + user_id-scoped from day one; every external fetch is isolated in a job/service layer, never in a render/query path. Lets storage migrate (D1->Turso->Postgres) later without touching domain logic. This is the 'publish someday' door-hinge (FR-48).
- (constraint) Cloudflare free-tier operating envelope the design must respect: 50 subrequests/invocation + 10ms CPU/invocation (I/O waits don't count). Consequence: the one-time SEED IMPORT (enrich ~344 games) exceeds 50 subrequests and runs OUT-OF-BAND as a script (matches UX 'no seed-import UI'). Steady-state incremental PS sync adds few new games -> fits. PS+ catalog fetch is paginated but small. Bulk/fan-out work that can't fit one invocation must chunk (Workflows/Queues) or run out-of-band.
- (decision) [ADOPTED] Games DB = IGDB (Twitch OAuth2 client-credentials, cacheable ~60d token refreshed via cron). Binds FR-23/24/26/41 (genres+covers+release dates). Resolves PRD open-q #3. Chosen for canonical genre taxonomy (the FR-23 consistency contract) + enthusiast cover art for the cover-forward shelf. RAWG deferred as the v1.x Metacritic score source (open-q #5) — not exclusive; enrichment can pull covers/genres from IGDB now, scores from RAWG later.
- (decision) [ADOPTED] PS auth v1 = proven pdccws_p session cookie (from export_ps_catalog.py) behind a provider seam. Binds FR-33/36/38. Resolves PRD open-q #2 conservatively. Cookie stored in settings table, editable in UI, read fresh per call; on 401/403 surface refresh instructions, never retry (NFR-4/FR-36). NPSSO/psn-api is a provider swap-in once a spike validates it authorizes the purchased-library persisted query — logged as a deferred spike, not a v1 dependency.
- (question) SPIKE (deferred, non-blocking): does an NPSSO-derived bearer token (psn-api) authorize getPurchasedGameList + the PS+ Extra catalog persisted queries? If yes, swap the PS auth provider from cookie to NPSSO refresh-token flow (less frequent re-auth). Revisit condition: cookie-refresh friction becomes annoying, or before any publish.
- (decision) [ADOPTED] PARADIGM = layered + ports-and-adapters at exactly two seams (persistence, external providers). Worker is the composition root. Layers: React SPA -> JSON API (routes) -> services/jobs -> domain core (pure, no I/O) + repositories (D1) + provider ports (PSN, IGDB). Domain core imports nothing with I/O. Not full hexagonal (single-user; avoid ceremony) — seams only where portability+testability pay off.
- (decision) [ADOPTED] AD-boundary-1 Domain core is I/O-free: no fetch, no D1 import; effective-state/derived-state/reconciliation/invariants are pure functions. Prevents: untestable domain, I/O leaking into logic. AD-boundary-2 Persistence only via repositories (D1 seam) — swappable to Turso/Postgres without touching domain (publish door-hinge). AD-boundary-3 External I/O only via provider ports (PSN, IGDB); cookie/NPSSO swap lives inside PSN adapter. AD-boundary-4 Nothing external on render (NFR-3) enforced structurally: read/query paths use repositories ONLY; providers touched only by ingest jobs (sync/import/PS+ check/add-by-name).
- (decision) [ADOPTED] STATE/DATA invariants: (5) effective-state computed in ONE domain fn (FR-8), consumed by ordering+labels+filters. (6) derived states never stored, always computed: Released/Wishlisted/Playable-now (FR-12/13/14). (7) ONE title-normalization fn shared by import+sync+add-by-name (FR-27/34/42). (8) append-only to user data at one write-path guard — no sync/import writes status/milestones/dates/genres on existing games (FR-33/45/6); membership-sourced PS entries filtered at ingest boundary, never create a game / never set Owned (FR-9/26/33).
- (decision) [ADOPTED] STATE/DATA invariants cont'd: (9) lifecycle dates write-once through automatic flows (FR-44/45), editable only in detail view. (10) FR-3 invariant (every game always has a play status OR >=1 completion milestone) enforced at domain/persistence boundary — API refuses an edit that would violate it. (11) every tracking-data row is user_id-scoped; every query filters by user_id (FR-48) — multi-user seam present from row one.
- (version) Web-verified 2026-07-05, toolchain fits Cloudflare+D1: Hono (Worker router + typed RPC client); Drizzle ORM 0.44.x + drizzle-kit (D1, generate SQL -> wrangler d1 migrations apply in CI, never at deploy); TanStack Query (client server-state); Zod; Vite + React + vite-plugin-pwa; Vitest + @cloudflare/vitest-pool-workers (runs in workerd with D1 migrations); Biome v2 (single lint+format tool).
- (decision) [ADOPTED] Toolchain slate + Biome. Rules set: (a) migrations run from CI (wrangler d1 migrations apply), never at deploy — Workers can't run Node at deploy; (b) UI/filter state = React built-in + URL params (no Redux/Zustand); server-state = TanStack Query; (c) out-of-band seed script writes D1 via D1 HTTP API/Wrangler using the same Drizzle schema; (d) Zod schema at API boundary shared SPA<->Worker.
- (decision) [ADOPTED] Operational envelope: deploy = Wrangler; environments = local (wrangler dev/miniflare + local D1) and production; secrets via Wrangler secrets/.env for IGDB(Twitch client id/secret) + initial PSN cookie seed, but the live pdccws_p cookie lives in a D1 settings table editable from the UI (read fresh per call). Scheduled work = Cloudflare Cron Trigger: monthly PS+ Extra refresh (FR-39) aligned to Sony's update; a failed run surfaces a notice on next app open (FR-40/NFR-4). CSV export (FR-49) is a Worker route streaming the full library from D1.
- (decision) [ADOPTED] SPA over SSR (confirms PRD default): single-screen, auth-gated, single-user, no SEO need; NFR-3 nothing-external-on-render fits a client-cached SPA. React SPA built by Vite, served as static assets by the same Worker that hosts the API.
- (event) Reviewer gate: lint 0 findings; rubric PASS-WITH-FIXES; incompatibility PASS-WITH-FIXES (2 CRITICAL data-model holes); tech-verification PASS-WITH-FIXES. No blockers. Applying all clear fixes.
- (decision) [ADOPTED] AD-17 GAME_TRACKING PK=(user_id,game_id), 1 row per (user,game) -> GAME 1:many GAME_TRACKING (fixes ERD 1:1 error; aligns AD-13). AD-18 title_normalized is a NON-UNIQUE candidate key; the EXTERNAL_LINK (source,external_id) is the identity anchor (reconciles AD-9 'only match key' with FR-34 'two games can normalize alike'). AD-20 EXTERNAL_LINK is many-per-(game,source) (PS4/PS5 collapse binds 2 PSN ids to one game); FR-34 conflict redefined as 'an external_id that resolves to a DIFFERENT game', flagged not merged.
- (decision) [ADOPTED] AD-19 attribute-ownership split: GAME holds shared catalog identity (title, title_normalized, release_date, cover_url, store_url, genres via game_genre, ps_plus_extra catalog-membership per region); GAME_TRACKING holds per-user mutable state (play_status, completed_on, platinum_on, started_on, bought_on, wishlisted_on, owned, ownership_type). Clarifies AD-8: derived-never-stored = Released/Wishlisted/Playable-now ONLY; cover_url/store_url and PS+Extra catalog membership are STORED INPUTS refreshed by ingest jobs (not derived, not user-writable), and Playable-now is derived FROM the stored PS+Extra fact.
- (decision) [ADOPTED] AD-21 single core milestone-WRITE reconciliation fn (symmetric to AD-7 read): logging completed_on/platinum_on auto-clears play_status to null per FR-2 uniformly; both the shelf popover and the detail view call this one fn — neither surface hand-rolls the side-effect. AD-22 'straggler' is a first-class needs-attention record: (a) import staging rows that couldn't match a game (carry Notion payload, not yet a GAME) and (b) name-only add-by-name entries (real GAME rows flagged unenriched). FR-30 Notion status-mapping is a pure core fn reusing the AD-9 normalizer; anything it can't place -> straggler, never guess. AD-23 PS+ Extra is per-region: account region stored in SETTING (or derived+persisted from PSN on sync); both manual + cron PS+ checks read it (cited in AD-5).
- (version) Corrections (web-verified 2026-07-05): Drizzle pin 0.45.x (0.45.2 latest; 0.44 stale). D1 binding calls DO count as subrequests (Cloudflare-services bucket, 1000/invocation) — AD-1 'not a subrequest' rationale removed. Free cap is 50 EXTERNAL subrequests + 1000 Cloudflare-services per invocation (AD-15 reworded; out-of-band-seed reasoning stands). Cron Triggers 3/Worker on free (only 1-2 needed). psn-api lacks getPurchasedGameList (v1 owned-library is raw GraphQL behind cookie — already isolated by AD-5, deferred spike).
- (decision) [ADOPTED] Operations posture (was thin): structured logging + wrangler tail over ingest jobs + cron runs; backup/DR = D1 Time Travel (point-in-time recovery) + the FR-49 CSV export as the user-held second copy. Added as an Operations note in the spine.
- (decision) [ADOPTED] Delivery model = trunk-based development (main is the trunk; short-lived branches; merge fast) + CI on every push (Biome + Vitest workers-pool + tsc) + continuous deployment on merge to main (wrangler d1 migrations apply -> wrangler deploy, per AD-16 order). Rationale: single-user/single-dev/free-tier — GitFlow-style release branches + tagged releases buy nothing until publish. Optional safety valve: manual-approval gate on the production deploy step for destructive migrations. Release branches/tags + a staging Wrangler environment are DEFERRED to the publish milestone (config change, not rework).
- (event) User reconfirmed Cloudflare+D1 over Convex (publish-scenario future-proofing lives in AD-4 repository seam + AD-13 user-scoping); no platform change.
- (event) spine finalized (status: final). 23 ADs, lint clean. project-context.md updated (Bun demotion, decided stack, conventions now owned by spine). All 3 reviewer gates PASS-WITH-FIXES, all fixes applied. Deliverable: ARCHITECTURE-SPINE.md.
- (event) Epic 7 / Story 7.0 design gate opened (2026-07-14): update this feature spine with the ps_plus_catalog data model + the cross-tree comms decision (Epic 6 retro action item 2).
- (constraint) PROBED LIVE (2026-07-14, categoryGridRetrieve, region de-de, totalCount 490): a catalog Product carries id, name, npTitleId, platforms[], media[] (IMAGE roles incl. MASTER/GAMEHUB_COVER_ART), price, storeDisplayClassification. It carries NO genres and NO release date. Epic 7 story 7.1's AC ('PS-store genres, release date' upserted per product) is therefore not satisfiable from the product payload as written.
- (constraint) PROBED LIVE: genres exist only as a category FACET (facetOptions[].name='productGenres', 19 locale-independent enum keys: ACTION 240, ADVENTURE 171, ROLE_PLAYING_GAMES 82, ... EDUCATIONAL 1). A filtered re-query filterBy:['productGenres:HORROR'] returns exactly that facet's 26 products. So per-game genre is obtainable ONLY by one filtered sweep per genre key, not from the product record.
- (decision) AD-24 ps_plus_catalog is a snapshot table, never a GAME. Binds: story 7.1/7.2/7.3, the 5.1 flag check. Prevents: a dev upserting catalog products into GAME (breaking 'availability is not ownership', AD-10) or hanging GAME_TRACKING off them. Rule: catalog products live in ps_plus_catalog keyed (region, tier, product_id) with tier DEFAULT 'extra'; columns are exactly what the payload gives (product_id, np_title_id, name, title_normalized per AD-9, cover_url, platforms, store_classification, store_url) + first_seen_at/last_seen_at. NO release_date column (not in the payload). A catalog row becomes a GAME only through the explicit 7.3 add path.
- (decision) AD-26 two genre vocabularies, never merged. Binds: catalog genre filter (7.2) vs the shelf genre filter (FR-23). Prevents: a dev writing PS facet keys into GENRE/GAME_GENRE (IGDB vocabulary, AD-19) so the shelf's genre pills silently gain 'ROLE_PLAYING_GAMES' next to 'Role-playing (RPG)'. Rule: PS-store genres are the 19 locale-independent productGenres facet KEYS, stored in ps_plus_catalog_genre (catalog-local, region+tier scoped). GENRE/GAME_GENRE stays IGDB-only. Display names are localized at render, never stored. On 7.3 add, the new GAME's genres come from IGDB enrichment, not from the catalog facet.
- (decision) AD-27 one catalog fetch feeds both the snapshot and the 5.1 flag check. Binds: runPsPlusCheck (services/psplus.ts), the cron path (5.2), story 7.1. Prevents: two fetch paths (one to persist, one to flag) drifting to different regions/pages/guards - the exact both-directions bug class AD-23 was written for. Rule: the ingest fetches ONCE, upserts+prunes ps_plus_catalog, and the tracked-game ps_plus_extra flag pass then reads the TABLE, not a second fetch. The existing empty-catalog wipe guard (psplus.ts:73) runs BEFORE any prune or clear - it now guards two datasets, so it stays a hard abort, not a warning.
- (decision) AD-28 the genre sweep is chunked and additive. Binds: 7.1 ingest under AD-15 (50 external subrequests/invocation). Prevents: a dev looping 19 genre queries + the base pass inline and tripping the free-tier cap under a full catalog (~490 products = ~5 base pages + ~25 genre pages). Rule: the base membership pass (products) and the per-genre tagging sweep are separate chunked passes with a resumable cursor (the story 9.3 backfill pattern). Genre tags are additive per pass; a genre pass that fails leaves the snapshot valid but partially tagged - never blocks the membership snapshot.
- (decision) AD-25 destinations and cross-tree intent travel through the router, never window events. Binds: web/ shell, shelf, catalog (7.2), detail. Prevents: the Epic 6 mount-race class (two live surfaces from one input; a listener not yet mounted swallowing an event) inheriting into a second destination. Rule: adopt react-router (v8, library/declarative mode). '/' Shelf, '/catalog' Catalog, '/game/:id' detail, '?q=' the search term. OPEN_DETAIL_EVENT, SEED_SEARCH_EVENT and SHELF_SEARCH_EVENT are DELETED, replaced by navigate()/useSearchParams. No new window CustomEvent may be added for cross-tree state - a new one is an architecture violation, not a judgment call. (Chosen by Luca 2026-07-14 over an app-level React context, which was cheaper but gave no deep links, no PWA back-button, and lost destination on reload.)
- (version) react-router 8.2.0 (verified on the web 2026-07-14): ESM-only, requires React >=19.2.7 / Vite >=7 / Node 22+. Project is on React 19.2.7 + Vite 8.1 - compatible at the floor, no React upgrade needed. Library mode only (no Vite plugin / framework mode). The Worker serves the SPA via Static Assets, so it needs SPA fallback (not_found_handling: single-page-application) or deep links 404 on reload.
- (question) Story 7.1's AC asks for release_date on catalog rows; the payload has none (only a productReleaseDate SORT/facet). Options: drop the column (spine's call - AD-24), or fetch it per-product (490 extra subrequests, rejected under AD-15). Flagged to the epic: 7.1's AC needs amending, not the architecture.
- (event) Reviewer gate run 2026-07-14 (lint clean + 2 lenses). Tech lens: react-router floor is Node >=22.22 not Vite (repo declares no engines, CI is Bun-only); SPA fallback ALREADY configured in wrangler.jsonc (keep run_worker_first /api/* carve-out); AD-28's 'trips the 50-subrequest cap' rationale was arithmetically FALSE (~30 of 50) - rewritten to partial-failure isolation + headroom. Adversarial lens: 6 divergence pairs found and closed - (1) PSN external_id namespace collision product_id vs np_title_id forcing mandatory duplicates -> AD-20 fixes source='PSN' to np_title_id only, product_id becomes source 'PSN_PRODUCT'; (2) two owners of PS+ membership (table vs game.ps_plus_extra, opposite answers for an OWNED catalog game) -> AD-27 makes the table sole truth, the flag a cache maintained for owned games too, all reads through one core fn; (3) a catalog add would derive Wishlisted for a playable game -> AD-8 amended (Wishlisted = not owned AND not in catalog), AD-24 fixes the exact tracking row 7.3 writes; (4) cron prune mid-sweep orphans genre rows and re-orders the cursor -> AD-28 gains snapshot generations + cascade delete; (5) one global SearchBox writing a shared ?q= reintroduces the two-surfaces-one-input bug THROUGH THE URL -> AD-25 scopes ?q= to the active destination; (6) /game/:id fed from the shelf list cache = same mount race with a hard 404 -> AD-25 mandates a by-id read route.
- (event) spine finalized (Epic 7 update: AD-24..AD-28 added; AD-8/AD-19/AD-20/AD-27 amended)
- (constraint) PROBED 2026-07-14 (SAMPLE-OF-ONE rule applied to the facet list): the productGenres facet key set is NOT a fixed 19-key constant - de-de returns 19 keys, en-us returns 20 (adds MUSIC/RHYTHM). It varies by region and MUST be discovered from the unfiltered response's facetOptions at ingest, never hardcoded. Keys are also not identifier-safe (MUSIC/RHYTHM contains a slash; it filters correctly only as a URL-encoded filterBy variable). AD-26 tightened accordingly. This is the same failure shape as story 9.3's npServiceName pinned from one PS5 sample.
- (override) CORRECTION 2026-07-14 (Luca challenged the claim; the code disproved me). I had amended AD-8 to 'Wishlisted = not owned AND not in the PS+ catalog' and specified the 7.3 add as {owned:false, ownership_type:'ps_plus'}. BOTH WERE WRONG, invented from the epic prose rather than read from the code. Reality: OWNERSHIP_TYPES = ['physical','digital'] (the FORMAT) — there is no 'ps_plus' value; the acquisition SOURCE is owned_via: 'purchase'|'membership'; a PS+ claim COUNTS AS OWNED with owned_via='membership' and never stamps bought_on (FR-9 amended 2026-07-11, un-claimed on cancel by story 6.4); and core/derived-state.ts:31 has shipped with playableNow = (owned || inPsPlusExtraCatalog) && released. The existing model ALREADY expresses the catalog case correctly: a catalog-added game is not owned -> Wishlisted AND Playable-now, which IS the Flow-2 pre-purchase signal ('I want it; the subscription covers it'). My amendment would have destroyed that signal and fought shelf.ts's owned-before-wishlisted sort tier. AD-8 REVERTED to its shipped rule; AD-24 now specifies the add as the existing not-owned default {owned:false, play_status:'Not started', wishlisted_on:today}. Lesson: do not amend a shipped derivation from an epic's prose without reading the core function.
- (constraint) VERIFIED SAFE (2026-07-14): AD-27 maintaining game.ps_plus_extra for OWNED games leaks no UI. Every surface renders the DERIVATION , not the raw flag — card badge (web/shelf/Card.tsx:155), PS+ filter pill (filters.ts:97, Shelf.tsx:214), and story 6.4's buy-vs-claim prompt (useTrackingMutations.ts:298, fires only while !owned). Making the stored flag truthful for owned games changes no rendered behavior; it only stops the stored fact from lying. The '&& !owned' guards are the display rule and must NOT be removed to compensate.
- (event) Epic 8 / Story 8.0 design gate opened (2026-07-17): multi-user auth model + data scoping + per-region refresh + free-tier budgets, sourced from sprint-change-proposal-2026-07-17-epic8-capacity.md and the capacity research (research/technical-cloudflare-free-tier-capacity-research-2026-07-17.md).
- (decision) [PROPOSED — awaiting Luca sign-off] AD-29 admission = stored allowlist table (invite-shaped, owner-managed, fail closed when empty), ONE rule at all four doors: magic-link pre-gate, OAuth create hook, OAuth account-LINK path, requireAuth + get-session. De-admission revokes session rows; auth endpoints rate-limited (verification-row residue). Registration-vs-invite was the gate's open product call — invite proposed (free-tier budgets + no moderation surface make open signup an unfunded liability); the sign-off decides.
- (decision) [PROPOSED — awaiting Luca sign-off] AD-30 the four PS+ columns on GAME (ps_plus_extra, ps_plus_left_on, ps_plus_leaving_on, psn_concept_id) are region-scoped facts in the wrong home: they move to the per-region snapshot side; per-user answers are DERIVATIONS (user region joined to region-scoped data via the one core membership fn, AD-27 extended) — never per-user copies. critic/user scores + ttb_* stay shared on GAME. 8.3 is the migration.
- (decision) [PROPOSED — awaiting Luca sign-off] AD-31 the cron fans out over DISTINCT REGIONS of registered users (anonymous fetch, shared snapshot; no account identity on the wire post-Epic 11). Region-state ledger (last_success/last_attempt/failure_count/cycle-complete), retry-failed-first, skip idle-60d or cycle-complete; sign-in vs >35d snapshot triggers waitUntil refresh + 'updating…' notice. Failures passive (logs + as-of staleness; amends AD-14's banner posture for this path). Manual check button removed in multi-user.
- (decision) [PROPOSED — awaiting Luca sign-off] AD-32 budget arithmetic enumerated (extends AD-15, per BUDGET-COUNTS-EVERY-SUBREQUEST): ~30 external subrequests per region per run → one region per invocation, ledger-ordered chunking; ~1,000 row-writes per 490-row snapshot → per-region monthly ≪ 100k/day vs per-user daily ≈ 100-user cliff; ~1,500 rows read per whole-library scan → ~550 DAU ceiling pre-8.6, ~6,600 after. Any new fan-out/scan states its arithmetic before merge.
- (decision) [PROPOSED — awaiting Luca sign-off] AD-33 read-budget delivery: single-row by-id reads, SQL COUNT(*), paged catalog delivery, per-active-region version-keyed cache (generation-keyed, AD-28), per-user library version for ETag/304, diff-based snapshot upserts, optional better-auth session.cookieCache. Single-tenant-safe; 8.6 may land before 8.2.
- (decision) AD-23 AMENDED: region is a per-user SETTING row (table already keyed (user_id,key)); env.PSN_REGION demoted to first-boot default. Deferred multi-tenant-door note amended to point at AD-29..33.
- (event) Reviewer gate run 2026-07-17 (2 lenses, adversarial + edge-case; 33 raw findings → 17 patched). Key corrections: (1) AD-32's '30 subrequests → one region per invocation' was disproved by the shipped H3 guard (membership alone = 34; passes never combine) — rewritten to AD-28's rotation-slot model, full region ≈ 7-8 slots, ~28 fires/month → 3-4-region monthly ceiling, named with its remedy (denser fires); DAU chain fixed (~9,000 rows/session → ~550; ~6,600 is the proposal's request-cap ceiling, research's enumerated fixes give ~2,000). (2) AD-30: departure facts CANNOT live in the pruned snapshot — mandatory (region, product_id) departure ledger surviving prunes; un-claim writer corrected (shipped 6.4, per-user tracking only). (3) AD-29: migration seeds the admission table from AUTH_ALLOWED_EMAIL (empty-table lockout), self/last-row removal refused, emails lowercased, five doors not four, WAF-not-D1 rate limiting, verification-row TTL sweep. (4) AD-31: 3-failure quarantine (poison-region starvation), last_user_activity ledger field (idle measured by activity not sign-ins), stale guard also fires on region change + first request of day, region-keyed single-flight lock, waitUntil revival = membership pass only, cycle-complete defined (all three passes within the 15-28 window), sweep/leaving state moves from per-user SETTING to the region ledger, 8.4 owns banner-machinery retirement. (5) AD-33: diff-based upserts REJECTED (breaks AD-28 generation-carried sweep/prune, saves ~1% of a day's writes — deliberate override of the change proposal); ETag bump invariant named; cookieCache TTL ≤5min or off (de-admission latency). The two facts in full, verbatim: (1) core/derived-state.ts line 31 ships wishlisted = !owned, and playableNow = (owned OR inPsPlusExtraCatalog) AND released. AD-8 is reverted to exactly that; Epic 7 changes no derivation. (2) AD-27 flagging owned games leaks no UI because every surface renders the derivation psPlusExtra AND NOT owned, never the raw stored flag: web/shelf/Card.tsx line 155 (badge), web/shelf/filters.ts line 97 and Shelf.tsx line 214 (PS+ pill), web/shelf/useTrackingMutations.ts line 298 (story 6.4 buy-vs-claim prompt, fires only while not owned). Those NOT-owned guards are the display rule and must not be removed to compensate.
