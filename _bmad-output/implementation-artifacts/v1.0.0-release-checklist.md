# v1.0.0 Release Checklist — PRESS START (`ps-game-catalog`)

Go-live gate for the first tagged release. App is feature-complete (Epics 1–6
`done`); this checklist covers the merge, deploy, and release hygiene.

> **Merging to `main` triggers the production deploy** (`.github/workflows/deploy.yml`
> fires on push to `main`: lint → typecheck → test → build → D1 migrate → deploy →
> smoke-test `/api/health` + `/api/auth/get-session`).

## Blockers

- [ ] **Merge `feat/epic-6/add-at-discovery` → `main`** via PR through the CI gate,
      merge-commit style (the merge dance). This is the release gate in
      `sprint-status.yaml`.
- [x] **4 GitHub repo secrets present** (CD reads these): `CLOUDFLARE_API_TOKEN`,
      `CLOUDFLARE_ACCOUNT_ID`, `BETTER_AUTH_SECRET`, `RESEND_API_KEY`. Verified via
      `gh secret list`.
- [ ] **Magic-link deliverability.** `AUTH_EMAIL_FROM = onboarding@resend.dev` only
      delivers to your own Resend account email. Confirm the login inbox receives it;
      for real inbox delivery verify a domain in Resend and update `AUTH_EMAIL_FROM`.

## Verify before go-live

- [ ] **`AUTH_ALLOWED_EMAIL` = `l.filice.89@gmail.com`** is the correct login account.
- [ ] **Prod D1 seeded** with catalog data:
      `wrangler d1 execute ps-game-catalog --remote --command "SELECT count(*) FROM game"`.
- [ ] **Manual-only worker secrets** (CD does NOT sync these):
      `wrangler secret put IGDB_CLIENT_ID` / `IGDB_CLIENT_SECRET` (absent → name-only
      adds). PSN cookie is settable in-app.
- [ ] **Cron** `0 21 15-21 * *` (PS+ Extra refresh) live as a `scheduled` handler.

## Release hygiene

- [ ] Bump `package.json` version `0.0.0` → `1.0.0`.
- [ ] Update stale README (still claims "walking skeleton, no auth/schema" +
      placeholder D1 id; both wrong — Epics 1–6 done, real D1 id committed).
- [ ] Tag `v1.0.0` and push after the release branch merges.
- [ ] Clean up leftover `.claude/worktrees/` agent directories.

## Post-deploy smoke

- [x] Pipeline curls `/api/health` + `/api/auth/get-session` automatically.
- [ ] Manual: request magic link → **receive email** → log in → catalog renders with
      data → add-a-game flow works.

## Out of scope for v1 (intentional, deferred)

- Publication blockers B1–B6 (single-email allowlist, global PS+ column, global PSN
  region, cron only for allowlist user) → Epic 8, demand-driven, post-v1.
- Epic 7 (browse PS+ catalog) → backlog.
- DW-9 Playwright flake watch → non-blocking.
