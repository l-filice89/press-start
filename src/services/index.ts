/**
 * Ingest jobs + orchestration (seed, PS sync, PS+ Extra check, add-by-name).
 * The only layer that touches both `repositories/` and `providers/` (AD-6).
 * Placeholder for Story 1.1 — real jobs land in Story 1.2+.
 */
export {};
